/* Salvar perfil do usuário (nome) */
document.addEventListener('click', e => {

})

const usuarioEventHandler = e => {
  switch(e.target.id) {
    case 'form__perfil-save--button':
      return () => {

      }
  }
}

const handleFormPerfilSaveButton = () => {

}